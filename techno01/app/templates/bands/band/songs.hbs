{{! template-lint-disable }}

<ul class="rr-list">
  {{#each model.songs as |song|}}
        <li>
          {{song.title}} by <em>{{song.band}}</em>
          <span class="rating fr">{{song.rating}}</span>
        </li>
      {{/each}}
</ul>